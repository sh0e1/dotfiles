# If local-specific settings are required, write them in this file.
#
# If you want to ignore the git diffs on this file, run the following command.
# git update-index --skip-worktree $XDG_CONFIG_HOME/zsh/local.zsh

# echo 'loaded local.zsh'
